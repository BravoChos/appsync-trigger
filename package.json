{
  "name": "appsync-trigger-lambda",
  "version": "1.0.0",
  "description": "Sending your GraphQL request to the AppSync endpoint with lambda",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "author": "Song Cho",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.604.0",
    "axios": "^0.19.1"
  }
}
